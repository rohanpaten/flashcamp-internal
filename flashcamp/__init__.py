"""
FlashCAMP - Startup success prediction system.
"""

__version__ = "1.0.0"

# Don't import app here to avoid circular imports
# The proper way to run the app is via the uvicorn command:
# uvicorn flashcamp.backend.main:app --reload
